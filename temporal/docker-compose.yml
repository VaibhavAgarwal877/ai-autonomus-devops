version: "3"

services:
  temporal:
    image: temporalio/auto-setup
    ports:
      - "7233:7233"

  temporal-ui:
    image: temporalio/ui
    ports:
      - "8088:8080"
    environment:
      - TEMPORAL_ADDRESS=temporal:7233